# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Dependency directories
node_modules/
jspm_packages/

# Artifacts
dist
build
built
lib

.parcel-cache

play.js
.turbo

# Runtime matrix: per-runtime results are CI artifacts, only the aggregated
# runtime-matrix.json is committed. The bundler entry is temporary.
results/
packages/std/__tests__/_matrix/bundle/
.test-bundle-entry.ts

.next
website/.dev
# Generated by the otfw build. Its route map hardcodes absolute paths, so a
# committed copy is machine-specific and fails `biome check`.
website/.otfw
website/.otfw-api
.otfw-api

