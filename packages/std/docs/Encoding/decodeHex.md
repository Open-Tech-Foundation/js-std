# decodeHex

Decodes a hexadecimal string to bytes.

@param {string} str The hexadecimal string to decode.
@returns {Uint8Array} The decoded bytes.

### Example

```js
decodeHex('48656c6c6f') //=> Uint8Array [72, 101, 108, 108, 111]
```
