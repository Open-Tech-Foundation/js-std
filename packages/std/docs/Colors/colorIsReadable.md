# colorIsReadable

Checks if the contrast between two colors meets WCAG standards.

## Parameters

- **An** `{ color1: ColorInput, color2: ColorInput, level?: AccessibilityLevel }` — object naming the two colors and the level.

## Returns

`boolean` — Whether the colors are readable.
