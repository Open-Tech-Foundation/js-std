# colorMix

Mixes two colors together.

## Parameters

- **An** `{ color1: ColorInput, color2: ColorInput, weight?: number, to?: ColorFormat }` — object naming the two colors, the weight and the format.

## Returns

`ColorOutput` — The mixed color.

## Throws

- `RangeError` — If the weight is not a finite number.
