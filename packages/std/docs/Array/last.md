# last

Returns the last element of the array.

## Parameters

- **arr** `T[]` — The source array.

## Returns

`T | undefined` — The last element, or undefined if empty.

## Example

```js
last([1, 2, 3]) //=> 3
last([]) //=> undefined
```
