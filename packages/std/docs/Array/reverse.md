# reverse

Reverses the order of elements in the given array.

## Parameters

- **arr** `T[]` — The source array.

## Returns

`T[]` — A new reversed array.

## Example

```js
reverse([1, 2, 3]) //=> [3, 2, 1]
```
