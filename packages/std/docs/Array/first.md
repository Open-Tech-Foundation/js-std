# first

Returns the first element of the array.

## Parameters

- **arr** `T[]` — The source array.

## Returns

`T | undefined` — The first element, or undefined if empty.

## Example

```js
first([1, 2, 3]) //=> 1
first([]) //=> undefined
```
