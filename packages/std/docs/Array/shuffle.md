# shuffle

Randomizes the order of the elements in a given array.

## Parameters

- **arr** `T[]` — The source array.

## Returns

`T[]` — A new shuffled array.

## Example

```js
shuffle([1, 2, 3]) //=> [2, 3, 1]
```
