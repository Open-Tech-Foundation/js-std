# isNumber

Checks if the given value is number.

### Example

```js

isNumber(1) //=> true

isNumber('1') //=> false

isNumber('1', true) //=> true

isNumber(NaN) //=> false

isNumber(Infinity) //=> false
```
