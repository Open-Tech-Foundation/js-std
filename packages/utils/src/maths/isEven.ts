/**
 * Checks if the given number is an Even number.
 *
 * @example
 *
 * isEven(1) //=> false
 * isEven(2) //=> true
 */

export default function isEven(n: number): boolean {
  return n % 2 === 0;
}
