import TryIt from '../../../components/TryIt.jsx';
export const tryItCode = "console.log(noop()); //=> undefined\n\n// Useful default callback\nconst { onComplete = noop } = options;";
export const metadata = {
  title: "noop — Functional",
  description: "A function that does nothing and returns undefined."
};

😶 A function that does nothing and returns undefined.

## Syntax

```ts
import { noop } from '@opentf/std';
noop(): undefined;
```

## Returns

`undefined`.

## Examples

```ts
noop(); //=> undefined

// Useful as a default callback
const { onComplete = noop } = options;
```

## Related

- [sleep](/docs/Timing/sleep)

## Try it

<TryIt code={tryItCode} />
