import TryIt from '../../../components/TryIt.jsx';
export const tryItCode = "console.log(percentageOf(0, 0)); //=> 0\n\nconsole.log(percentageOf(1, 1)); //=> 0.01\n\nconsole.log(percentageOf(10, 80)); //=> 8\n\nconsole.log(percentageOf(50, 900)); //=> 450\n\nconsole.log(percentageOf(17.5, 10)); //=> 1.75";
export const metadata = {
  title: "percentageOf — Maths",
  description: "Calculates the value of a given percentage of a number."
};

💹 Calculates the value of a given percentage of a number.

## Syntax

```ts
import { percentageOf } from '@opentf/std';
percentageOf(percentage: number, num: number): number
```

## Parameters

- `percentage`: The percentage value.
- `num`: The number to calculate the percentage of.

## Returns

The calculated value.

## Examples

```ts
percentageOf(0, 0); //=> 0

percentageOf(1, 1); //=> 0.01

percentageOf(10, 80); //=> 8

percentageOf(50, 900); //=> 450

percentageOf(17.5, 10); //=> 1.75
```

## Related

- [percentage](/docs/Maths/percentage)

<TryIt code={tryItCode} />
