import TryIt from '../../../components/TryIt.jsx';
export const tryItCode = "console.log(prod([])); //=> 1\n\nconsole.log(prod([1, 2, 3, 4, 5])); //=> 120\n\nconsole.log(prod([1, -1])); //=> -1\n\nconsole.log(prod([-1, -2])); //=> 2\n\nconst objects = [{ n: 4 }, { n: 2 }, { n: 8 }];\nconsole.log(prod(objects, (v) => v.n)); //=> 64";
export const metadata = {
  title: "prod — Maths",
  description: "Calculates the product of all numbers in an array."
};

✖️ Calculates the product of all numbers in an array.

## Syntax

```ts
import { prod } from '@opentf/std';
prod<T>(
  arr: T[] = [],
  cb?: (val: T, index: number) => number
): number
```

## Parameters

- `arr`: An array of numbers.
- `cb`: An optional iteratee invoked for each element to generate the value to be multiplied.

## Returns

The product of all elements. Returns `1` for empty arrays.

## Examples

```ts
prod([]) //=> 1

prod([1, 2, 3, 4, 5]) //=> 120

prod([1, -1]) //=> -1

prod([-1, -2]) //=> 2

const objects = [{ n: 4 }, { n: 2 }, { n: 8 }];
prod(objects, (v) => v.n) //=> 64
```

## Related

- [sum](/docs/Maths/sum)

<TryIt code={tryItCode} />
