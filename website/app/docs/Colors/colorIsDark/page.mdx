import TryIt from '../../../components/TryIt.jsx';
export const tryItCode = "console.log(colorIsDark('black')); //=> true\nconsole.log(colorIsDark('white')); //=> false\nconsole.log(colorIsDark('navy')); //=> true";
export const metadata = {
  title: "colorIsDark — Colors",
  description: "Checks if a color is dark based on its relative luminance."
};

🌙 Checks if a color is dark based on its relative luminance.

## Syntax

```ts
import { colorIsDark } from '@opentf/std';
colorIsDark(input: any): boolean
```

Returns `true` if the luminance is less than `0.5`.

## Examples

```ts
colorIsDark('black'); //=> true
colorIsDark('white'); //=> false
colorIsDark('navy'); //=> true
```

<TryIt code={tryItCode} />
