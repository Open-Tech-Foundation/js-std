import TryIt from '../../../components/TryIt.jsx';
export const tryItCode = "console.log(max([])); //=> null\n\nconsole.log(max([1, undefined, 2, null, 3])); //=> 3\n\nconsole.log(max([1, 2, -3, 4, 5])); //=> 5\n\nconsole.log(max(['a', 'b', 'c'])); //=> 'c'\n\nconsole.log(max(['apple', 'mango', 'grapes'])); //=> 'mango'\n\nconst arr = [\n  {\n    name: 'x',\n    age: 10,\n  },\n  {\n    name: 'y',\n    age: 16,\n  },\n  {\n    name: 'z',\n    age: 13,\n  },\n  { name: 'y2', age: 16 },\n];\nmax(arr, (f) => f.age)\n//=>\n// {\n//  name: 'y',\n//  age: 16,\n// }";
import { Callout } from "@opentf/web-docs";
export const metadata = {
  title: "max — Array",
  description: "Returns the maximum value in an array."
};


📈 Returns the maximum value in an array.

<Callout type="info">
  If duplicate maximum values are present, the first occurrence is returned.
</Callout>

## Syntax

```ts
import { max } from '@opentf/std';
max<T>(
  arr: T[] = [],
  by: (val: T) => number | string = (x: T) => x as unknown as number | string
): T | null
```

## Parameters

- `arr`: The source array.
- `by`: An optional function to extract the numeric or string value for comparison. Defaults to identity function.

## Returns

The maximum value or `null` if the array is empty (after compacting).

## Examples

```ts
max([]) //=> null

max([1, undefined, 2, null, 3]) //=> 3

max([1, 2, -3, 4, 5]) //=> 5

max(['a', 'b', 'c']) //=> 'c'

max(['apple', 'mango', 'grapes']) //=> 'mango'

const arr = [
  {
    name: 'x',
    age: 10,
  },
  {
    name: 'y',
    age: 16,
  },
  {
    name: 'z',
    age: 13,
  },
  { name: 'y2', age: 16 },
];
max(arr, (f) => f.age)
//=>
// {
//  name: 'y',
//  age: 16,
// }
```

## Related

- [min](/docs/Array/min)
- [bounds](/docs/Array/bounds)

## Try it

<TryIt code={tryItCode} />
