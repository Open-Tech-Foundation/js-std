import TryIt from '../../../components/TryIt.jsx';
export const tryItCode = "const arr = [1, 2, 3];\nconsole.log(intersperse(arr, '&')); //=> [1, '&', 2, '&', 3]\n\nconst menu = ['Home', 'Menu', 'Sub Menu'];\nconsole.log(intersperse(menu, '>'));\n//=> [\n//  'Home',\n//  '>',\n//  'Menu',\n//  '>',\n//  'Sub Menu',\n// ]\n\nconsole.log(intersperse('Hello', '-')); //=> 'H-e-l-l-o'\n\nconst items = [\n  { type: 'div', text: 'Apple' },\n  { type: 'div', text: 'Orange' },\n  { type: 'div', text: 'Mango' },\n];\nconsole.log(intersperse(items, { type: 'br' }));\n//=> [\n//   {\n//     type: 'div',\n//     text: 'Apple',\n//   },\n//   {\n//     type: 'br',\n//   },\n//   {\n//     type: 'div',\n//     text: 'Orange',\n//   },\n//   {\n//     type: 'br',\n//   },\n//   {\n//     type: 'div',\n//     text: 'Mango',\n//   },\n// ]\n\nconsole.log(intersperse(['a', 'b', 'c'], (i) => `<span key=\"sep-${i}\"> , </span>`));\n//=> [\n//   'a',\n//   '<span key=\"sep-0\"> , </span>',\n//   'b',\n//   '<span key=\"sep-1\"> , </span>',\n//   'c',\n// ]";
import { Callout } from "@opentf/web-docs";
export const metadata = {
  title: "intersperse — Array",
  description: "Inserts a separator between each element of an array or characters of a string."
};


🧵 Inserts a separator between each element of an array or characters of a string.

<Callout type="info">
  The `list` can be an **Array** or a **String**.
</Callout>

## Syntax

```ts
import { intersperse } from '@opentf/std';
intersperse(
  list: string | unknown[],
  sep: ((index: number) => unknown) | unknown
): string | unknown[]
```

## Parameters

- `list`: The source array or string.
- `sep`: The separator to insert. Can be a value or a function that returns a value based on the current index.

## Returns

A new array or string with the separator interspersed.

## Examples

```ts
const arr = [1, 2, 3];
intersperse(arr, '&') //=> [1, '&', 2, '&', 3]

const menu = ['Home', 'Menu', 'Sub Menu'];
intersperse(menu, '>')
//=> [
//  'Home',
//  '>',
//  'Menu',
//  '>',
//  'Sub Menu',
// ]

intersperse('Hello', '-') //=> 'H-e-l-l-o'

const items = [
  { type: 'div', text: 'Apple' },
  { type: 'div', text: 'Orange' },
  { type: 'div', text: 'Mango' },
];
intersperse(items, { type: 'br' })
//=> [
//   {
//     type: 'div',
//     text: 'Apple',
//   },
//   {
//     type: 'br',
//   },
//   {
//     type: 'div',
//     text: 'Orange',
//   },
//   {
//     type: 'br',
//   },
//   {
//     type: 'div',
//     text: 'Mango',
//   },
// ]

intersperse(['a', 'b', 'c'], (i) => `<span key="sep-${i}"> , </span>`)
//=> [
//   'a',
//   '<span key="sep-0"> , </span>',
//   'b',
//   '<span key="sep-1"> , </span>',
//   'c',
// ]
```

## Related

- [range](/docs/Array/range)

## Try it

<TryIt code={tryItCode} />
