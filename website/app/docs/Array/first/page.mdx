import TryIt from '../../../components/TryIt.jsx';
export const tryItCode = "const users = [\n  { name: 'Ada', role: 'admin' },\n  { name: 'Lin', role: 'editor' },\n];\n\n// Grab the first admin, or fall back to a default\nconst admin = first(users.filter((u) => u.role === 'admin'));\nconsole.log(admin); //=> { name: 'Ada', role: 'admin' }\n\n// Safe on empty arrays — returns undefined instead of throwing\nconsole.log(first([])); //=> undefined";
export const metadata = {
  title: "first — Array",
  description: "Returns the first element of the array."
};

🎯 Returns the first element of the array.

## Syntax

```ts
import { first } from '@opentf/std';
first<T>(arr: T[] = []): T | undefined
```

## Parameters

- `arr`: The source array.

## Returns

The first element, or `undefined` if the array is empty.

## Examples

```ts
first([1, 2, 3]) //=> 1

first(['a', 'b', 'c']) //=> 'a'

first([]) //=> undefined

first() //=> undefined
```

## Related

- [last](/docs/Array/last)

<TryIt code={tryItCode} />
