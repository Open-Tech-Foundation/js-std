import TryIt from '../../../components/TryIt.jsx';
export const tryItCode = "console.log(last([1, 2, 3])); //=> 3\n\nconsole.log(last(['a', 'b', 'c'])); //=> 'c'\n\nconsole.log(last([])); //=> undefined\n\nconsole.log(last()); //=> undefined";
export const metadata = {
  title: "last — Array",
  description: "Returns the last element of the array."
};

🎯 Returns the last element of the array.

## Syntax

```ts
import { last } from '@opentf/std';
last<T>(arr: T[] = []): T | undefined
```

## Parameters

- `arr`: The source array.

## Returns

The last element, or `undefined` if the array is empty.

## Examples

```ts
last([1, 2, 3]) //=> 3

last(['a', 'b', 'c']) //=> 'c'

last([]) //=> undefined

last() //=> undefined
```

## Related

- [first](/docs/Array/first)

## Try it

<TryIt code={tryItCode} />
