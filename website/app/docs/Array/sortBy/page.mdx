import TryIt from '../../../components/TryIt.jsx';
export const tryItCode = "const fruits = [\n  { name: 'banana', amount: 2 },\n  { name: 'apple', amount: 4 },\n  { name: 'pineapple', amount: 2 },\n  { name: 'mango', amount: 1 },\n  { name: 'orange', amount: 5 },\n];\n\nsortBy(fruits, ['name', 'asc']);\n// OR\nconsole.log(sortBy(fruits, [(o) => o.name, 'asc']));\n//=> [\n//   { name: 'apple', amount: 4 },\n//   { name: 'banana', amount: 2 },\n//   { name: 'mango', amount: 1 },\n//   { name: 'orange', amount: 5 },\n//   { name: 'pineapple', amount: 2 },\n// ]\n\nsortBy(fruits, ['amount', 'desc'], ['name', 'asc']);\n// OR\nconsole.log(sortBy(fruits, [o => o.amount, 'desc'], [o => o.name, 'asc']));\n//=> [\n//  { name: 'orange', amount: 5 },\n//  { name: 'apple', amount: 4 },\n//  { name: 'banana', amount: 2 },\n//  { name: 'pineapple', amount: 2 },\n//  { name: 'mango', amount: 1 }\n// ]";
import { Callout } from "@opentf/web-docs";
export const metadata = {
  title: "sortBy — Array",
  description: "Sorts an array of objects by one or more properties or iteratees."
};


📊 Sorts an array of objects by one or more properties or iteratees.

<Callout emoji="✅" type="info">
  **Immutable**: This function does not mutate the original array.
</Callout>

## Syntax

```ts
import { sortBy } from '@opentf/std';
sortBy<T>(
  arr: T[],
  ...tuples: [key: keyof T | ((obj: T) => unknown), order: "asc" | "desc"][]
): T[]
```

## Parameters

- `arr`: The source array of objects.
- `...tuples`: A list of tuples where each tuple contains a key (property name or function) and the sort order (`'asc'` or `'desc'`).

## Returns

A new sorted array.

## Examples

```ts
const fruits = [
  { name: 'banana', amount: 2 },
  { name: 'apple', amount: 4 },
  { name: 'pineapple', amount: 2 },
  { name: 'mango', amount: 1 },
  { name: 'orange', amount: 5 },
];

sortBy(fruits, ['name', 'asc']);
// OR
sortBy(fruits, [(o) => o.name, 'asc']);
//=> [
//   { name: 'apple', amount: 4 },
//   { name: 'banana', amount: 2 },
//   { name: 'mango', amount: 1 },
//   { name: 'orange', amount: 5 },
//   { name: 'pineapple', amount: 2 },
// ]

sortBy(fruits, ['amount', 'desc'], ['name', 'asc']);
// OR
sortBy(fruits, [o => o.amount, 'desc'], [o => o.name, 'asc']);
//=> [
//  { name: 'orange', amount: 5 },
//  { name: 'apple', amount: 4 },
//  { name: 'banana', amount: 2 },
//  { name: 'pineapple', amount: 2 },
//  { name: 'mango', amount: 1 }
// ]
```

## Related

- [sort](/docs/Array/sort)

## Try it

<TryIt code={tryItCode} />
