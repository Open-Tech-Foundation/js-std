import { Callout } from "@opentf/web-docs";

🔀 Randomizes the order of elements in an array using the [Fisher–Yates shuffle](https://en.wikipedia.org/wiki/Fisher%E2%80%93Yates_shuffle) algorithm.

<Callout emoji="✅" type="info">
  **Immutable**: This function does not mutate the original array.
</Callout>

## Syntax

```ts
import { shuffle } from '@opentf/std';

shuffle<T>(arr: T[]): T[]
```

## Parameters

- `arr`: The source array to shuffle.

## Returns

A new array with the elements in randomized order.

## Examples

```ts
shuffle([]) //=> []

shuffle([1]) //=> [1]

shuffle([1, 2, 3, 4, 5]) //=> [2, 4, 5, 1, 3] (random result)

shuffle('Apple') //=> ['p', 'e', 'A', 'l', 'p'] (random result)
```

## Related

- [reverse](/docs/Array/reverse)
- [sort](/docs/Array/sort)

