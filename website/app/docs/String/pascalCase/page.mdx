import TryIt from '../../../components/TryIt.jsx';
export const tryItCode = "console.log(pascalCase('Foo Bar')); //=> 'FooBar'\n\nconsole.log(pascalCase('--foo-bar--')); //=> 'FooBar'\n\nconsole.log(pascalCase('__FOO_BAR__')); //=> 'FooBar'";
export const metadata = {
  title: "pascalCase — String",
  description: "Converts string to pascal case."
};

🐫 Converts string to pascal case.

## Syntax

```ts
import { pascalCase } from '@opentf/std';
pascalCase(str: string): string;
```

## Parameters

- `str`: The string to convert.

## Returns

The pascal-cased string.

## Examples

```ts
pascalCase('Foo Bar') //=> 'FooBar'

pascalCase('--foo-bar--') //=> 'FooBar'

pascalCase('__FOO_BAR__') //=> 'FooBar'
```

## Related

- [camelCase](/docs/String/camelCase)
- [kebabCase](/docs/String/kebabCase)
- [snakeCase](/docs/String/snakeCase)

## Try it

<TryIt code={tryItCode} />
