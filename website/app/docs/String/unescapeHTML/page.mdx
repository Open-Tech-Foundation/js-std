import TryIt from '../../../components/TryIt.jsx';
export const tryItCode = "unescapeHTML('&lt;script&gt;alert(&quot;xss&quot;)&lt;/script&gt;')\n//=> '<script>alert(\"xss\")</script>'";
export const metadata = {
  title: "unescapeHTML — String",
  description: "Unescapes HTML entities back to their original characters."
};

🔓 Unescapes HTML entities back to their original characters.

## Syntax

```ts
import { unescapeHTML } from '@opentf/std';
unescapeHTML(str: string): string;
```

## Returns

The unescaped string.

Supports both `&#39;` and `&apos;` for apostrophes.

## Examples

```ts
unescapeHTML('&lt;script&gt;alert(&quot;xss&quot;)&lt;/script&gt;')
//=> '<script>alert("xss")</script>'
```

## Related

- [escapeHTML](/docs/String/escapeHTML)

## Try it

<TryIt code={tryItCode} />
