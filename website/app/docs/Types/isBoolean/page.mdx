export const metadata = {
  title: "isBoolean — Types",
  description: "Checks if the given value is Boolean."
};

Checks if the given value is [Boolean](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Boolean).

## Syntax

```ts
import { isBoolean } from '@opentf/std';
isBoolean(val: unknown): val is boolean;
```

## Examples

```ts
isBoolean(true) //=> true

isBoolean(false) //=> true

isBoolean(null) //=> false

isBoolean(1) //=> false

isBoolean() //=> false
```

## Related

- [isObject](/docs/Types/isObject)
- [isPlainObject](/docs/Types/isPlainObject)

## Try it

```ts
console.log(isBoolean(true)); //=> true

console.log(isBoolean(false)); //=> true

console.log(isBoolean(null)); //=> false

console.log(isBoolean(1)); //=> false

console.log(isBoolean()); //=> false
```
