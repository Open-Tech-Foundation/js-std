export const metadata = {
  title: "isUndefined — Types",
  description: "Checks if the given value is undefined."
};

Checks if the given value is [undefined](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/undefined).

## Syntax

```ts
import { isUndefined } from '@opentf/std';
isUndefined(value: unknown): val is undefined
```

## Examples

```ts
isUndefined() //=> true

isUndefined(undefined) //=> true

isUndefined(null) //=> false
```

## Related

- [isObject](/docs/Types/isObject)
- [isPlainObject](/docs/Types/isPlainObject)

## Try it

```ts
console.log(isUndefined()); //=> true

console.log(isUndefined(undefined)); //=> true

console.log(isUndefined(null)); //=> false
```
