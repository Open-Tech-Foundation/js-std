export const metadata = {
  title: "isWeakSet — Types",
  description: "Checks if the given value is WeakSet."
};

Checks if the given value is [WeakSet](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/WeakSet).

## Syntax

```ts
import { isWeakSet } from '@opentf/std';
isWeakSet(value: unknown): val is WeakSet<WeakKey>
```

## Examples

```ts
isWeakSet(new Set()) //=> false

isWeakSet(new WeakSet()) //=> true
```

## Related

- [isObject](/docs/Types/isObject)
- [isPlainObject](/docs/Types/isPlainObject)

## Try it

```ts
console.log(isWeakSet(new Set())); //=> false

console.log(isWeakSet(new WeakSet())); //=> true
```
