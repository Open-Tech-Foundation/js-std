import TryIt from '../../../components/TryIt.jsx';
export const tryItCode = "console.log(isBlob(new Blob())); //=> true\n\nconsole.log(isBlob({})); //=> false";
export const metadata = {
  title: "isBlob — Types",
  description: "Checks if the given value is a Blob."
};

Checks if the given value is a [Blob](https://developer.mozilla.org/en-US/docs/Web/API/Blob).

## Syntax

```ts
import { isBlob } from '@opentf/std';
isBlob(val: unknown): val is Blob
```

## Examples

```ts
isBlob(new Blob()) //=> true

isBlob({}) //=> false
```

## Related

- [isObject](/docs/Types/isObject)
- [isPlainObject](/docs/Types/isPlainObject)

## Try it

<TryIt code={tryItCode} />
