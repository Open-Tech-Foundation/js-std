import TryIt from '../../../components/TryIt.jsx';
export const tryItCode = "console.log(isWeakMap(new Map())); //=> false\n\nconsole.log(isWeakMap(new WeakMap())); //=> true";
export const metadata = {
  title: "isWeakMap — Types",
  description: "Checks if the given value is WeakMap."
};

Checks if the given value is [WeakMap](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/WeakMap).

## Syntax

```ts
import { isWeakMap } from '@opentf/std';
isWeakMap(value: unknown): val is WeakMap<WeakKey, unknown>
```

## Examples

```ts
isWeakMap(new Map()) //=> false

isWeakMap(new WeakMap()) //=> true
```

## Related

- [isObject](/docs/Types/isObject)
- [isPlainObject](/docs/Types/isPlainObject)

## Try it

<TryIt code={tryItCode} />
