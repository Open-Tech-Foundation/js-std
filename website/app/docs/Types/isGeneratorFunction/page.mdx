import TryIt from '../../../components/TryIt.jsx';
export const tryItCode = "console.log(isGeneratorFunction(function() {})); //=> false\n\nconsole.log(isGeneratorFunction({})); //=> false\n\nfunction* generator(i) {\n  yield i;\n  yield i + 10;\n}\n\nconsole.log(isGeneratorFunction(generator)); //=> true\n\nasync function* aGen(i) {\n  yield i;\n  yield i + 10;\n}\n\nconsole.log(isGeneratorFunction(aGen)); //=> false\n\nconst someObj = {\n  *generator() {\n    yield \"a\";\n    yield \"b\";\n  },\n};\n\nconsole.log(isGeneratorFunction(someObj.generator)); //=> true";
export const metadata = {
  title: "isGeneratorFunction — Types",
  description: "Checks if the given value is a Generator Function."
};

Checks if the given value is a [Generator Function](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/function*).

## Syntax

```ts
import { isGeneratorFunction } from '@opentf/std';
isGeneratorFunction(
  val: unknown
): val is GeneratorFunction
```

## Examples

```ts
isGeneratorFunction(function() {}) //=> false

isGeneratorFunction({}) //=> false

function* generator(i) {
  yield i;
  yield i + 10;
}

isGeneratorFunction(generator); //=> true

async function* aGen(i) {
  yield i;
  yield i + 10;
}

isGeneratorFunction(aGen); //=> false

const someObj = {
  *generator() {
    yield "a";
    yield "b";
  },
};

isGeneratorFunction(someObj.generator); //=> true
```

## Related

- [isObject](/docs/Types/isObject)
- [isPlainObject](/docs/Types/isPlainObject)

## Try it

<TryIt code={tryItCode} />
