export const metadata = {
  title: "isInfinity — Types",
  description: "Checks if the given value is Infinity."
};

Checks if the given value is [Infinity](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Infinity).

## Syntax

```ts
import { isInfinity } from '@opentf/std';
isInfinity(value: unknown): x is typeof Infinity
```

## Examples

```ts
isInfinity(0) //=> false

isInfinity(false) //=> false

isInfinity(null) //=> false

isInfinity(NaN) //=> false

isInfinity(1/0) //=> true

isInfinity(Infinity) //=> true

isInfinity(-Infinity) //=> true
```

## Related

- [isObject](/docs/Types/isObject)
- [isPlainObject](/docs/Types/isPlainObject)

## Try it

```ts
console.log(isInfinity(0)); //=> false

console.log(isInfinity(false)); //=> false

console.log(isInfinity(null)); //=> false

console.log(isInfinity(NaN)); //=> false

console.log(isInfinity(1/0)); //=> true

console.log(isInfinity(Infinity)); //=> true

console.log(isInfinity(-Infinity)); //=> true
```
