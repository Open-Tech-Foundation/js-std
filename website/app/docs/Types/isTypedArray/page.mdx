import TryIt from '../../../components/TryIt.jsx';
export const tryItCode = "console.log(isTypedArray([])); //=> false\n\nconsole.log(isTypedArray(new Int8Array())); //=> true\n\nconsole.log(isTypedArray(new Float64Array())); //=> true";
export const metadata = {
  title: "isTypedArray — Types",
  description: "Checks if the given value is TypedArray."
};

Checks if the given value is [TypedArray](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/TypedArray).

## Syntax

```ts
import { isTypedArray } from '@opentf/std';
isTypedArray(value: unknown): val is TypedArray
```

## Examples

```ts
isTypedArray([]) //=> false

isTypedArray(new Int8Array()) //=> true

isTypedArray(new Float64Array()) //=> true
```

## Related

- [isObject](/docs/Types/isObject)
- [isPlainObject](/docs/Types/isPlainObject)

<TryIt code={tryItCode} />
