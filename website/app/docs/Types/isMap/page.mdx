import TryIt from '../../../components/TryIt.jsx';
export const tryItCode = "console.log(isMap(new Map())); //=> true\n\nconsole.log(isMap({})); //=> false";
export const metadata = {
  title: "isMap — Types",
  description: "Checks if the given value is a Map object."
};

Checks if the given value is a [Map](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Map) object.

## Syntax

```ts
import { isMap } from '@opentf/std';
isMap(val: unknown): val is Map<unknown, unknown>
```

## Examples

```ts
isMap(new Map()) //=> true

isMap({}) //=> false
```

## Related

- [isObject](/docs/Types/isObject)
- [isPlainObject](/docs/Types/isPlainObject)

## Try it

<TryIt code={tryItCode} />
