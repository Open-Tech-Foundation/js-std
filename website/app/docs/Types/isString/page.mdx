import TryIt from '../../../components/TryIt.jsx';
export const metadata = {
  title: "isString — Types",
  description: "Checks if the given value is String."
};

Checks if the given value is [String](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String).

## Syntax

```ts
import { isString } from '@opentf/std';
isString(value: unknown): val is string
```

## Examples

```ts
isString('abc') //=> true

isString(String(1)) //=> true

isString(1) //=> false
```

## Related

- [isObject](/docs/Types/isObject)
- [isPlainObject](/docs/Types/isPlainObject)

<TryIt code={"console.log(isString('abc')); //=> true\n\nconsole.log(isString(String(1))); //=> true\n\nconsole.log(isString(1)); //=> false"} />
