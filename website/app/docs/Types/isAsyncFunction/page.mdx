import TryIt from '../../../components/TryIt.jsx';
export const tryItCode = "console.log(isAsyncFunction(function() {})); //=> false\n\nconsole.log(isAsyncFunction({})); //=> false\n\nfunction* generator(i) {\n  yield i;\n  yield i + 10;\n}\n\nconsole.log(isAsyncFunction(generator)); //=> false\n\nconst afn = async () => {}\nconsole.log(isAsyncFunction(afn)); //=> true\n\nasync function getUsers() {}\nconsole.log(isAsyncFunction(getUsers)); //=> true\n\nasync function* aGen(i) {\n  yield i;\n  yield i + 10;\n}\n\nconsole.log(isAsyncFunction(aGen)); //=> false";
export const metadata = {
  title: "isAsyncFunction — Types",
  description: "Checks if the given value is an AsyncFunction."
};

Checks if the given value is an [AsyncFunction](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/AsyncFunction).

## Syntax

```ts
import { isAsyncFunction } from '@opentf/std';
isAsyncFunction(
  val: unknown
): val is (...args: unknown[]) => Promise<unknown>
```

## Examples

```ts
isAsyncFunction(function() {}) //=> false

isAsyncFunction({}) //=> false

function* generator(i) {
  yield i;
  yield i + 10;
}

isAsyncFunction(generator); //=> false

const afn = async () => {}
isAsyncFunction(afn) //=> true

async function getUsers() {}
isAsyncFunction(getUsers) //=> true

async function* aGen(i) {
  yield i;
  yield i + 10;
}

isAsyncFunction(aGen); //=> false
```

## Related

- [isObject](/docs/Types/isObject)
- [isPlainObject](/docs/Types/isPlainObject)

<TryIt code={tryItCode} />
