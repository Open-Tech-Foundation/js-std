import TryIt from '../../../components/TryIt.jsx';
export const tryItCode = "console.log(isSymbol(Symbol())); //=> true\n\nconsole.log(isSymbol(Symbol('abc'))); //=> true\n\nconsole.log(isSymbol('abc')); //=> false";
export const metadata = {
  title: "isSymbol — Types",
  description: "Checks if the given value is Symbol."
};

Checks if the given value is [Symbol](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Symbol).

## Syntax

```ts
import { isSymbol } from '@opentf/std';
isSymbol(value: unknown): val is symbol
```

## Examples

```ts
isSymbol(Symbol()) //=> true

isSymbol(Symbol('abc')) //=> true

isSymbol('abc') //=> false
```

## Related

- [isObject](/docs/Types/isObject)
- [isPlainObject](/docs/Types/isPlainObject)

<TryIt code={tryItCode} />
