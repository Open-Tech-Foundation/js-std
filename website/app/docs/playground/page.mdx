import { Callout } from "@opentf/web-docs";

import PlaygroundRepl from "../../components/PlaygroundRepl.jsx";

<div style="text-align: center">
### Playground
</div>

<PlaygroundRepl />

<Callout type="note">
  Press `CTRL + Enter` in the editor to run the code. Code runs in a sandboxed iframe with a Web Worker (browser build of `@opentf/std`).
</Callout>