import { Callout } from "@opentf/web-docs";
export const metadata = {
  title: "shallowMergeAll — Object",
  description: "Shallow merges objects and concatenates the array."
};


Shallow merges objects and concatenates the array.

## Syntax

```ts
import { shallowMergeAll } from '@opentf/std';
shallowMergeAll(obj1, ...objN);
```

<Callout type="info" emoji="✅">
Immutable: This does not mutate the given arrays or objects.
</Callout>

<Callout type="warning" emoji="🛡️">
  Sources are copied without `Object.assign`, so an own `__proto__` key cannot replace
  the result's prototype.
</Callout>

## Examples

```ts
const a = [1];
const b = [2];
const c = [3];
shallowMergeAll(a, b, c); //=> [1, 2, 3]
```

