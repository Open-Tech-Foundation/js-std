import TryIt from '../../../components/TryIt.jsx';
import { Callout } from "@opentf/web-docs";
export const metadata = {
  title: "size — Object",
  description: "Returns the size (length, count, or byte length) of a collection."
};


📏 Returns the size (length, count, or byte length) of a collection.

<Callout type="info">
  The following types are supported:
  - **String**: Number of characters.
  - **Array**: Number of elements.
  - **Plain Object**: Number of enumerable own string-keyed and symbol-keyed properties.
  - **Map & Set**: Number of entries.
  - **ArrayBuffer, TypedArray, & DataView**: Byte length or number of elements.
</Callout>

<Callout type="warning">
  Returns `-1` if the value is an unsupported type (e.g., `null`, `undefined`, `number`, `boolean`).
</Callout>

## Syntax

```ts
import { size } from '@opentf/std';
size(val: unknown): number
```

## Parameters

- `val`: The value to measure.

## Returns

The size of the collection, or `-1` if unsupported.

## Examples

```ts
// --- Strings ---
size('') //=> 0
size('abc') //=> 3

// --- Arrays ---
size([]) //=> 0
size([1, 2, 3]) //=> 3

// --- Objects ---
size({}) //=> 0
size({ a: 1, b: 2 }) //=> 2
size({ [Symbol('x')]: 1 }) //=> 1

// --- Maps & Sets ---
size(new Map([['a', 1]])) //=> 1
size(new Set([1, 2])) //=> 2

// --- Buffers ---
size(new ArrayBuffer(8)) //=> 8
size(new Uint8Array(10)) //=> 10

// --- Unsupported ---
size(null) //=> -1
size(123) //=> -1
```

<TryIt code={"// --- Strings ---\nconsole.log(size('')); //=> 0\nconsole.log(size('abc')); //=> 3\n\n// --- Arrays ---\nconsole.log(size([])); //=> 0\nconsole.log(size([1, 2, 3])); //=> 3\n\n// --- Objects ---\nconsole.log(size({})); //=> 0\nconsole.log(size({ a: 1, b: 2 })); //=> 2\nconsole.log(size({ [Symbol('x')]: 1 })); //=> 1\n\n// --- Maps & Sets ---\nconsole.log(size(new Map([['a', 1]]))); //=> 1\nconsole.log(size(new Set([1, 2]))); //=> 2\n\n// --- Buffers ---\nconsole.log(size(new ArrayBuffer(8))); //=> 8\nconsole.log(size(new Uint8Array(10))); //=> 10\n\n// --- Unsupported ---\nconsole.log(size(null)); //=> -1\nconsole.log(size(123)); //=> -1"} />
