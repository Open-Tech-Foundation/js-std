import { Callout } from "@opentf/web-docs";
export const metadata = {
  title: "mapValues — Object",
  description: "Creates an object with the same keys and values generated by running each own enumerable string keyed and symbol keyed property of object through the callback."
};


Creates an object with the same keys and values generated by running each own enumerable string keyed and symbol keyed property of object through the callback.

<Callout type="info">
The source object should be a `Plain Object`.
</Callout>

<Callout type="info">
The returned object preserves a null prototype when the source object was created with `Object.create(null)`.
</Callout>

## Syntax

```ts
import { mapValues } from "@opentf/std";
mapValues(obj: object, fn: (value: any, key: string | symbol) => any)
```

<Callout type="warning" emoji="🛡️">
  `__proto__`, `constructor` and `prototype` are skipped.
</Callout>

## Examples

```ts
const obj = { a: 1, b: 2 };

mapValues(obj, (v) => v * 2); 
//=> { a: 2, b: 4 }
```
