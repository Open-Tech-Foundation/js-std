import REPL from "../../components/REPL";

> Checks whether the given string is a valid [JSON](https://json.org/) object.

## Syntax

```ts
import { isJSON } from '@opentf/std';

isJSON(str: string): boolean;
```

## Examples

```ts
// Falsy cases
// ***********

isJSON(""); //=> false

isJSON("undefined"); //=> false

isJSON("null"); //=> false

isJSON("true"); //=> false

isJSON("false"); //=> false

isJSON("1"); //=> false

isJSON("12345"); //=> false

isJSON("abc"); //=> false

isJSON("{a}"); //=> false

isJSON("{a:}"); //=> false

isJSON("{a:1}"); //=> false

isJSON('{"a": undefined}'); //=> false

isJSON("[]"); //=> false

// Truthy cases
// ************

isJSON("{}"); //=> true

isJSON('{"a":1}'); //=> true

isJSON(
  '{"a": "abc", "b": true, "c": false, "d": [{"e": 1}, null, 5.0], "f": {}}'
); //=> true
```

## Try

<REPL code={`const { isJSON } = require('@opentf/std');

log(isJSON('{a: 1}'));
isJSON('{"a": 1}');
`} />