import REPL from "../../components/REPL";

> Checks if the given value is a [Function](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Function).

## Syntax

```ts
import { isFn } from '@opentf/std';

isFn(val: unknown): boolean;
```

## Examples

```ts
isFn(function() {}) //=> true

isFn({}) //=> false

function* generator(i) {
  yield i;
  yield i + 10;
}

isFn(generator); //=> true
```

## Try

<REPL code={`const { isFn } = require('@opentf/std');

function a() {}

isFn(a);
`} />