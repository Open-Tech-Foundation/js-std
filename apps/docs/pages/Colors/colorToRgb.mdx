import REPL from "../../components/REPL";

> Converts an RGBA object to an rgb/rgba string.

## Syntax

```ts
import { colorToRgb } from '@opentf/std';

colorToRgb(color: { r: number, g: number, b: number, a: number }): string;
```

## Examples

```ts
colorToRgb({ r: 255, g: 0, b: 0, a: 1 }) //=> 'rgb(255, 0, 0)'

colorToRgb({ r: 255, g: 0, b: 0, a: 0.5 }) //=> 'rgba(255, 0, 0, 0.5)'
```

## Try

<REPL code={`const { colorToRgb } = require('@opentf/std');

colorToRgb({ r: 255, g: 0, b: 0, a: 1 });
`} />
