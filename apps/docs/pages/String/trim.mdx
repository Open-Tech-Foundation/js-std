import { Callout } from "nextra/components";
import REPL from "../../components/REPL";

> ✂️ Removes leading and trailing whitespace or specified characters from a string.

## Syntax

```ts copy
import { trim } from '@opentf/std';

trim(str: string, chars?: string): string;
```

## Parameters

- `str`: The string to trim.
- `chars` (Optional): The characters to trim. Defaults to whitespace.

## Returns

The trimmed string.

## Examples

```ts
trim('  abc  ') //=> 'abc'

trim('-_-abc-_-', '_-') //=> 'abc'
```

## Related

- [truncate](/String/truncate)
- [words](/String/words)

## Try

<REPL code={`const { trim } = require('@opentf/std');

console.log(trim('  Open Tech Foundation  '));
console.log(trim('!!!Hello!!!', '!'));
`} />
