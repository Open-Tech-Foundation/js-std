import { Callout } from "nextra/components";
import REPL from "../../components/REPL";

> Returns a boolean  indicating if this set has no elements in common with the given set.

<Callout type="info">
  The set can be either an `Array` or `Set`.
</Callout>

### Venn Diagram
<div style={{padding: '15px', backgroundColor: 'white'}}>
  ![Venn Diagram](/assets/isDisjointFrom-venn-diagram.svg)
  <div style={{textAlign: 'right', fontSize: '14px'}}>
    [Source: MDN](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Set)
  </div>
</div>

**Related**
- [diff](/Array/diff)
- [intersection](/Array/intersection)
- [symDiff](/Array/symDiff)
- [union](/Array/union)
- [isSubsetOf](/Maths/isSubsetOf)
- [isSupersetOf](/Maths/isSupersetOf)


## Syntax

```ts
import { isDisjointFrom } from '@opentf/std';

isDisjointFrom(
  a: unknown[] | Set<unknown>,
  b: unknown[] | Set<unknown>
): boolean
```

## Examples

```ts
isDisjointFrom([1, 3, 5], [2, 4]) //=> true

const primes = new Set([2, 3, 5, 7, 11, 13, 17, 19]);
const squares = new Set([1, 4, 9, 16]);
isDisjointFrom(primes, squares) //=> true

isDisjointFrom([1, 2, 3, 5], [2, 4]) //=> false

const composites = new Set([4, 6, 8, 9, 10, 12, 14, 15, 16, 18]);
const squares = new Set([1, 4, 9, 16]);

isDisjointFrom(composites, squares) //=> false
```

## Try

<REPL code={`const { isDisjointFrom } = require('@opentf/std');

isDisjointFrom([1, 3, 5], [2, 4]);
`} />