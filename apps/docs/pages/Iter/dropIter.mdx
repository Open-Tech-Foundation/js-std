import { Callout } from "nextra/components";
import REPL from "../../components/REPL";

> 🏃‍♂️ Skips the first N elements of an iterable.

## Syntax

```ts copy
import { dropIter } from '@opentf/std';

dropIter<T>(
  iterable: Iterable<T>,
  n: number = 1
): Generator<T>;
```

## Parameters

- `iterable`: The source iterable (Array, Set, Map, etc.).
- `n`: The number of elements to skip. Default: `1`.

## Returns

A Generator that yields the remaining elements.

## Examples

```ts
const arr = [1, 2, 3, 4];
const iter = dropIter(arr, 2);
[...iter]; //=> [3, 4]
```

## Related

- [takeIter](/Iter/takeIter)
- [dropIterAsync](/Iter/dropIterAsync)

## Try

<REPL code={`const { dropIter } = require('@opentf/std');

const iter = dropIter([1, 2, 3, 4, 5], 3);
console.log([...iter]);
`} />
