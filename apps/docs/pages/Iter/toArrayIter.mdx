import { Callout } from "nextra/components";
import REPL from "../../components/REPL";

> 🧩 Collects all items from an iterator into an array.

## Syntax

```ts copy
import { toArrayIter } from '@opentf/std';

toArrayIter<T>(iter: Iterable<T>): T[]
```

## Parameters

- `iter`: The source iterable.

## Returns

An array of all items.

## Examples

```ts
toArrayIter((function*() { yield 1; yield 2; })()) //=> [1, 2]
```

## Related

- [toArrayIterAsync](/Iter/toArrayIterAsync)

## Try

<REPL code={`const { toArrayIter } = require('@opentf/std');

toArrayIter((function*() { yield 1; yield 2; })());`} />
