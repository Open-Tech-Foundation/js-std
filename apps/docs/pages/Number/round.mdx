import REPL from "../../components/REPL";

## Syntax

```ts copy
import { round } from '@opentf/std';

round(n: number, decimals?: number): number;
```

## Parameters

| Name     | Type     | Description                                |
| -------- | -------- | ------------------------------------------ |
| n        | `number` | The number to round.                       |
| decimals | `number` | The number of decimal places (default 0).  |

## Returns

The rounded number without floating point drift.

## Examples

```ts
round(1.005, 2) //=> 1.01
round(1.23456, 3) //=> 1.235
```

## Related

- [isZero](/Number/isZero)

## Try

<REPL code={`const { round } = require('@opentf/std');

console.log(round(1.005, 2));
console.log(round(1.23456, 3));
`} />
