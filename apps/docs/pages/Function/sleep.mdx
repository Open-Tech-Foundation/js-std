import { Callout } from "nextra/components";
import REPL from "../../components/REPL";

> 😴 Suspends execution for a specified number of milliseconds.

## Syntax

```ts copy
import { sleep } from '@opentf/std';

sleep(ms: number = 0): Promise<void>
```

## Parameters

- `ms`: The number of milliseconds to sleep. Defaults to `0`.

## Returns

A `Promise` that resolves after the specified delay.

## Examples

```ts
await sleep(1000); // Suspends execution for 1 second

await sleep(0); // Resolves in the next event loop iteration

await sleep(); // Defaults to 0ms
```

## Try

<REPL code={`const { sleep } = require('@opentf/std');

async function main() {
  console.log('Start');
  await sleep(1000);
  console.log('End (after 1s)');
}

main();
`} />
