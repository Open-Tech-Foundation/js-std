import { Callout } from "nextra/components";
import REPL from "../../components/REPL";

> ⛓ Performs left-to-right function composition.

## Syntax

```ts copy
import { pipe } from '@opentf/std';

pipe(...funcs: Function[]): (val: any) => any;
```

## Parameters

- `...funcs`: The functions to pipe.

## Returns

A new function that represents the piping of the input functions.

## Examples

```ts
const add2 = (n) => n + 2;
const times3 = (n) => n * 3;

const times3ThenAdd2 = pipe(times3, add2);
times3ThenAdd2(5); //=> 17 ( (5 * 3) + 2 )
```

## Related

- [compose](/Functional/compose)
- [pipeFunction](/Functional/pipeFunction)

## Try

<REPL code={`const { pipe } = require('@opentf/std');

const add = (n) => n + 1;
const double = (n) => n * 2;
const result = pipe(double, add)(5);
console.log(result); // (5 * 2) + 1 = 11
`} />
